[Unit]
Description=Renew Viewer IP TLS certificate
After=docker.service network-online.target
Wants=network-online.target
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/renew-viewer-ip-certificate
